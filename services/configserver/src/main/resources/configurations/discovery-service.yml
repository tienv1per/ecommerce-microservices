eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false # not register with Eureka service
    fetch-registry: false # not fetch data from Eureka service
    service-url:
      default-zone: http://${eureka.instance.hostname}/${server.port}/eureka